# LayerSentry guest lifecycle state is root-owned in V1 because provider mutations
# still require privileged execution. A future split helper may introduce a
# dedicated unprivileged API account without changing durable state ownership.
