diff --git a/cmd/cloudstack-csi-driver/Dockerfile b/cmd/cloudstack-csi-driver/Dockerfile
index 00ef7a3..e80e170 100644
--- a/cmd/cloudstack-csi-driver/Dockerfile
+++ b/cmd/cloudstack-csi-driver/Dockerfile
@@ -1,4 +1,7 @@
-FROM --platform=$BUILDPLATFORM golang:1.23-alpine AS builder
+ARG GO_BUILDER_IMAGE=docker.io/library/golang@sha256:383395b794dffa5b53012a212365d40c8e37109a626ca30d6151c8348d380b5f
+ARG RUNTIME_IMAGE=docker.io/library/alpine@sha256:48b0309ca019d89d40f670aa1bc06e426dc0931948452e8491e3d65087abc07d
+
+FROM --platform=$BUILDPLATFORM ${GO_BUILDER_IMAGE} AS builder

 ARG TARGETOS
 ARG TARGETARCH
@@ -17,25 +20,25 @@ COPY . .
 RUN CGO_ENABLED=0 GOOS=${TARGETOS} GOARCH=${TARGETARCH} \
     go build -ldflags "${LDFLAGS}" -o cloudstack-csi-driver ./cmd/cloudstack-csi-driver

-FROM alpine:3.21
+FROM ${RUNTIME_IMAGE}

 LABEL \
     org.opencontainers.image.description="CloudStack CSI driver" \
     org.opencontainers.image.source="https://github.com/cloudstack/cloudstack-csi-driver/"

-RUN apk add --no-cache \
-    ca-certificates \
-    # Provides mkfs.ext2, mkfs.ext3, mkfs.ext4 (used by k8s.io/mount-utils)
-    e2fsprogs \
-    e2fsprogs-extra \
-    # Provides mkfs.xfs
-    xfsprogs \
-    # blkid, mount and umount are required by k8s.io/mount-utils \
-    blkid \
-    mount \
-    umount \
-    # Provides udevadm for device path detection \
-    udev
+ARG TARGETARCH
+RUN --network=none --mount=type=bind,source=.layersentry-apk,target=/locked \
+    set -eu; \
+    case "${TARGETARCH}" in \
+      amd64) expected=9445ecfd3c403aac3ba27de994c472d96e1b62452b54355361752d61cfb0489b ;; \
+      arm64) expected=a407ff73ad8264eaad421f014edae0dcbe9f03f1bb8d0bc6d2a69a7536ddac57 ;; \
+      *) echo "Unsupported locked APK architecture" >&2; exit 1 ;; \
+    esac; \
+    cd "/locked/${TARGETARCH}/driver"; \
+    echo "${expected}  SHA256SUMS" | sha256sum -c -; \
+    sha256sum -c SHA256SUMS; \
+    test "$(find . -maxdepth 1 -name '*.apk' | wc -l)" -eq "$(wc -l < SHA256SUMS)"; \
+    apk add --no-network --no-cache --repositories-file /dev/null ./*.apk

 COPY --from=builder /workspace/cloudstack-csi-driver /cloudstack-csi-driver
 ENTRYPOINT ["/cloudstack-csi-driver"]
diff --git a/cmd/cloudstack-csi-sc-syncer/Dockerfile b/cmd/cloudstack-csi-sc-syncer/Dockerfile
index f825ddc..530eaf6 100644
--- a/cmd/cloudstack-csi-sc-syncer/Dockerfile
+++ b/cmd/cloudstack-csi-sc-syncer/Dockerfile
@@ -1,4 +1,7 @@
-FROM --platform=$BUILDPLATFORM golang:1.23-alpine AS builder
+ARG GO_BUILDER_IMAGE=docker.io/library/golang@sha256:383395b794dffa5b53012a212365d40c8e37109a626ca30d6151c8348d380b5f
+ARG RUNTIME_IMAGE=docker.io/library/alpine@sha256:48b0309ca019d89d40f670aa1bc06e426dc0931948452e8491e3d65087abc07d
+
+FROM --platform=$BUILDPLATFORM ${GO_BUILDER_IMAGE} AS builder

 ARG TARGETOS
 ARG TARGETARCH
@@ -17,13 +20,25 @@ COPY . .
 RUN CGO_ENABLED=0 GOOS=${TARGETOS} GOARCH=${TARGETARCH} \
     go build -ldflags "${LDFLAGS}" -o cloudstack-csi-sc-syncer ./cmd/cloudstack-csi-sc-syncer

-FROM alpine:3.21
+FROM ${RUNTIME_IMAGE}

 LABEL \
     org.opencontainers.image.description="CloudStack disk offering to Kubernetes storage class syncer" \
     org.opencontainers.image.source="https://github.com/cloudstack/cloudstack-csi-driver/"

-RUN apk add --no-cache ca-certificates
+ARG TARGETARCH
+RUN --network=none --mount=type=bind,source=.layersentry-apk,target=/locked \
+    set -eu; \
+    case "${TARGETARCH}" in \
+      amd64) expected=a929de9bb04205887e36abc34e4891556f38c9a6e5ead964e3f2b6d0beaa16a0 ;; \
+      arm64) expected=3157cf1c022648f235d6849528393bba04988e42b5520613cee648e1246389fb ;; \
+      *) echo "Unsupported locked APK architecture" >&2; exit 1 ;; \
+    esac; \
+    cd "/locked/${TARGETARCH}/syncer"; \
+    echo "${expected}  SHA256SUMS" | sha256sum -c -; \
+    sha256sum -c SHA256SUMS; \
+    test "$(find . -maxdepth 1 -name '*.apk' | wc -l)" -eq "$(wc -l < SHA256SUMS)"; \
+    apk add --no-network --no-cache --repositories-file /dev/null ./*.apk

 COPY --from=builder /workspace/cloudstack-csi-sc-syncer /cloudstack-csi-sc-syncer
-ENTRYPOINT ["/cloudstack-csi-sc-syncer"]
\ No newline at end of file
+ENTRYPOINT ["/cloudstack-csi-sc-syncer"]
diff --git a/pkg/cloud/volumes.go b/pkg/cloud/volumes.go
index 3db152f..56f8089 100644
--- a/pkg/cloud/volumes.go
+++ b/pkg/cloud/volumes.go
@@ -161,6 +161,12 @@ func (c *client) ExpandVolume(ctx context.Context, volumeID string, newSizeInGB
 	currentSize := volume.Size
 	currentSizeInGB := util.RoundUpBytesToGB(currentSize)
 	volumeName := volume.Name
+	if currentSizeInGB >= newSizeInGB {
+		logger.V(2).Info("CloudStack volume already satisfies expansion request", "volumeID", volumeID,
+			"currentSize", currentSizeInGB, "requestedSize", newSizeInGB)
+
+		return nil
+	}
 	p := c.Volume.NewResizeVolumeParams(volumeID)
 	p.SetId(volumeID)
 	p.SetSize(newSizeInGB)
diff --git a/pkg/driver/controller.go b/pkg/driver/controller.go
index 9ca1185..72cc799 100644
--- a/pkg/driver/controller.go
+++ b/pkg/driver/controller.go
@@ -689,7 +689,7 @@ func (cs *controllerServer) ControllerExpandVolume(ctx context.Context, req *csi
 		return nil, status.Error(codes.OutOfRange, "Volume size exceeds the limit specified")
 	}

-	_, err := cs.connector.GetVolumeByID(ctx, volumeID)
+	volume, err := cs.connector.GetVolumeByID(ctx, volumeID)
 	if err != nil {
 		if errors.Is(err, cloud.ErrNotFound) {
 			return nil, status.Errorf(codes.NotFound, "Volume %v not found", volumeID)
@@ -706,6 +706,27 @@ func (cs *controllerServer) ControllerExpandVolume(ctx context.Context, req *csi
 	}
 	defer cs.operationLocks.ReleaseExpandLock(volumeID)

+	nodeExpansionRequired := true
+	// Node expansion is not required for raw block volumes.
+	volCap := req.GetVolumeCapability()
+	if volCap != nil && volCap.GetBlock() != nil {
+		nodeExpansionRequired = false
+	}
+
+	requestedSizeBytes := util.GigaBytesToBytes(volSizeGB)
+	if volume.Size >= requestedSizeBytes {
+		logger.Info("Volume already satisfies expansion request",
+			"volumeID", volumeID,
+			"currentSizeBytes", volume.Size,
+			"requestedSizeBytes", requestedSizeBytes,
+		)
+
+		return &csi.ControllerExpandVolumeResponse{
+			CapacityBytes:         volume.Size,
+			NodeExpansionRequired: nodeExpansionRequired,
+		}, nil
+	}
+
 	err = cs.connector.ExpandVolume(ctx, volumeID, volSizeGB)
 	if err != nil {
 		return nil, status.Errorf(codes.Internal, "Could not resize volume %q to size %v: %v", volumeID, volSizeGB, err)
@@ -716,13 +737,6 @@ func (cs *controllerServer) ControllerExpandVolume(ctx context.Context, req *csi
 		"volumeSize", volSizeGB,
 	)

-	nodeExpansionRequired := true
-	// Node expansion is not required for raw block volumes.
-	volCap := req.GetVolumeCapability()
-	if volCap != nil && volCap.GetBlock() != nil {
-		nodeExpansionRequired = false
-	}
-
 	return &csi.ControllerExpandVolumeResponse{
 		CapacityBytes:         util.GigaBytesToBytes(volSizeGB),
 		NodeExpansionRequired: nodeExpansionRequired,
diff --git a/pkg/driver/controller_test.go b/pkg/driver/controller_test.go
index a939f28..daa2479 100644
--- a/pkg/driver/controller_test.go
+++ b/pkg/driver/controller_test.go
@@ -20,9 +20,13 @@
 package driver

 import (
+	"context"
 	"testing"

 	"github.com/container-storage-interface/spec/lib/go/csi"
+
+	"github.com/cloudstack/cloudstack-csi-driver/pkg/cloud/fake"
+	"github.com/cloudstack/cloudstack-csi-driver/pkg/util"
 )

 func TestDetermineSize(t *testing.T) {
@@ -59,3 +63,33 @@ func TestDetermineSize(t *testing.T) {
 		})
 	}
 }
+
+func TestControllerExpandVolumeIsIdempotent(t *testing.T) {
+	ctx := context.Background()
+	connector := fake.New()
+	volumeID, err := connector.CreateVolume(ctx, "offering-1", "zone-1", "idempotent-volume", 5)
+	if err != nil {
+		t.Fatalf("CreateVolume failed: %v", err)
+	}
+	server := NewControllerServer(connector)
+	req := &csi.ControllerExpandVolumeRequest{
+		VolumeId: volumeID,
+		CapacityRange: &csi.CapacityRange{
+			RequiredBytes: util.GigaBytesToBytes(2),
+		},
+		VolumeCapability: &csi.VolumeCapability{
+			AccessType: &csi.VolumeCapability_Mount{Mount: &csi.VolumeCapability_MountVolume{}},
+		},
+	}
+
+	response, err := server.ControllerExpandVolume(ctx, req)
+	if err != nil {
+		t.Fatalf("ControllerExpandVolume failed: %v", err)
+	}
+	if response.CapacityBytes != util.GigaBytesToBytes(5) {
+		t.Fatalf("expected observed capacity, got %d", response.CapacityBytes)
+	}
+	if !response.NodeExpansionRequired {
+		t.Fatal("filesystem volume must preserve node expansion requirement")
+	}
+}
