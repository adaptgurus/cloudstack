# LayerSentry — Historical Codex Launcher Compatibility Stub

**Status:** `SUPERSEDED_FOR_EXECUTION`

The filename `CODEX_4_AGENT_RUNBOOK.md` is historical and no longer describes the LayerSentry execution model.

Current Codex launcher:

`docs/layersentry/LAYERSENTRY_CODEX_EXECUTION_RUNBOOK.md`

Current mandatory startup:

```text
/AGENTS.md
 -> LAYERSENTRY_EXECUTION_CONTRACT.md
 -> LAYERSENTRY_CURRENT_STATUS.md
 -> assigned workstream/module status
 -> actual current Git/workflow/live state
```

Do not infer a four-agent Codex model from this filename. The default current model is one standing K8s Codex source writer, an optional test-only healthy-RKE2 package lane, and one deferred final UI Codex pass. Other modules follow the current Execution Contract unless explicitly reassigned.

The previous content of this file remains available in Git history for audit purposes.