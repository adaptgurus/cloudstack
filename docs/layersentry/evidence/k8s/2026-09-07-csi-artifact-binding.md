# CSI artifact binding and closure reconciliation

Status: DESIGN_DEFINED. Base: `482c523305`. Scope: retained unsigned CloudStack CSI 3.0.2 driver/syncer OCI evidence and release artifact metadata only. The integration lead approved this scope after fresh GitHub/source inspection. No provider/installer/UI/service or lab changes are authorized in this task.

The successful existing workflow `34047140051` built both linux/amd64 images from LayerSentry source `d40c54acb8a18b71475b154f14dd631460c94f89` and exact upstream `a84477e922d62b82387ab55134fafc9c0b5aaf64`. Its artifact remains available, and its exact local archives are retained. Rebuilding would create unnecessary new identities. The selected approach re-verifies those bytes and records the archive, OCI index, runtime manifest/config, platform and unsigned SBOM/provenance bindings. The release candidate's `image: null` remains correct because an OCI archive or temporary build tag is not a proven registry pull reference. All existing live/project/resize/data-safety and readiness booleans remain unchanged.

The existing archive verifier checked content reachability and attestation subjects but accepted a syntactically valid source SHA without comparing it to the actual provenance predicate. The narrow correction must verify the exact exported index/platform shape and provenance's source revision, Dockerfile directory, LayerSentry version build argument, and approved base/scanner dependencies. These are consistency checks on unsigned build evidence, not cryptographic builder identity or production provenance certification. Negative tests must reject source substitution, descriptor/platform confusion and disconnected or mismatched attestations.

The release candidate and downstream manifest will reference one checksum-bound non-runtime artifact lock. Future publication must preserve the full OCI index and every referenced blob (including attestations), then independently verify the destination registry's index and runtime digests before creating an immutable pull reference. No public push or signing is performed. Air-gap dependency closure, project-scoped PVC lifecycle/expansion, retained-data survival during Machine replacement, backup/restore and stateful DBaaS remain separate E0/live gates.

## Verification and handoff

Status: CI_VERIFIED for retained artifact consistency only. Fresh GitHub API evidence still reports original run `34047140051` successful at exact `d40c54acb8a18b71475b154f14dd631460c94f89`, artifact `9993465630` unexpired through `2026-09-20T17:00:38Z`. Both locally retained archives were independently streamed/hashed again and matched the previously recorded archive and runtime digests. Driver OCI index is `sha256:863a4d6280fb2d354071c8fef7020297985e506a6f7a4fbce38ecb34a0357b43`; syncer index is `sha256:7deba98bf6851481ded002d13779f4ef43f666f6385dbfce2291037b5cafca73`. Each retains exactly one linux/amd64 runtime and one unknown/unknown attestation manifest referencing its runtime. SBOM package counts remain 98 and 67 respectively; both provenance predicates bind exact LayerSentry/upstream build arguments and three approved base/scanner dependency digests. Full identities and unsigned status are in the checksum-bound downstream artifact lock. No artifact download, rebuild, registry publication, signing or live operation was required for this reconciliation.

All 12 focused CSI closure/verifier tests passed, including false source attribution, incorrect descriptor platform/dependency, detached index, substituted archive/index/attestation lock fields, corrupt blobs, absent SBOM and wrong attestation subject. Both actual retained archives passed the stricter verifier and artifact binding. JSON checks prove the release candidate's `image:null`, hard gates, production restrictions and existing CSI booleans are unchanged. The `apkPackageLayerDeterministic:false` aggregate readiness field is deliberately not promoted; the separate original CI artifact evidence records the actual offline checksum/signature-checked APK build.

Exact-source inspection of `charts/cloudstack-csi/templates/{csi-controller-deploy,csi-node-ds,syncer-job}.yaml` and `values.yaml` in the retained upstream checkout found repository/tag composition and tag-only sidecar defaults: provisioner v5.0.1, attacher v4.6.1, resizer v1.11.1, livenessprobe v2.12.0 and node-driver-registrar v2.10.1. These are observed historical inputs, not a new Kubernetes 1.36 compatibility recommendation. No current executor consumes `ComponentReadiness.csi_image` for workload CSI installation. The integration lead owns the separate chart/sidecar/registry/secret/StorageClass integration and subsequent actual PVC lifecycle, resize/idempotency, Machine-replacement data survival and restore tests. Stateful DBaaS remains blocked. Rollback of this metadata/verifier change must preserve the existing OCI archives and live journals; no runtime rollback is needed because no runtime changed.
