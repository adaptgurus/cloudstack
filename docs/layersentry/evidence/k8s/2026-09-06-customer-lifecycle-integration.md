# Customer Kubernetes lifecycle integration

Status: SOURCE_COMPLETE for project-scoped collection APIs; live qualification remains NOT_TESTED. Base: fetched integration 42eb481719. The user reassigned this session to DC/DR and Kubernetes provisioning/DBaaS/APaaS completion. Work occurs in isolated branches; CloudStack Java, schema and KVM orchestration remain unchanged.

The existing E BFF supports create/detail/scale/delete and durable operation lookup, but cannot rediscover a customer's clusters or operations after browser state loss. The GUI only previews a local plan. Retain CAPI/CAPC/CAPRKE2 ownership and extend the existing BFF with authorized inventory and a separate same-origin JSON browser client. Do not add a second cluster state database, query all tenants and filter in the browser, or trust static release flags. Native CloudStack remains the discovery/RBAC authority; XaaS/native CKS do not own this selected RKE2 lifecycle.

Cluster inventory uses deterministic project namespaces and the existing LayerSentry project/managed labels, bounded native Kubernetes list pagination, and verifies every returned object's ownership. Operation inventory uses parameterized project-scoped SQLite reads and immutable keyset pagination. Authenticate and authorize before either query; reject duplicate/unknown query fields, malformed cursors and unbounded limits. Customer-visible state comes from current provider observations; journal existence is not proof that a cluster still exists. List failures remain failures rather than empty success.

Evidence reviewed: exact checked-out CloudStack 4.22.1.1 listProjects/session implementation and E controller source; version-pinned E architecture/addendum and prior upstream compatibility audit; Kubernetes API list/continue contract (https://v1-36.docs.kubernetes.io/docs/reference/using-api/api-concepts/). The existing downstream CAPC endpoint/volume patches and release tuple are retained. Provider architecture alternatives were already evaluated in the mandatory addendum; this change does not reopen that decision.

Threat boundary: the BFF holds privileged Kubernetes/provider access, while browser project IDs/cursors/labels are untrusted. Cross-project reads, selector injection, replay after refresh, ambiguous mutations and stale scope responses need negative tests. Existing session, origin and mutation idempotency checks remain enforced. No credentials enter browser configuration or evidence. Storage safety gates remain false until real E0 tests pass.

Verification plan: source tests for direct-API project isolation, list pagination and ownership, restart recovery, malformed input and provider failure; GUI tests for duplicate clicks, scope changes, bounded polling and error display; then exact-artifact Rocky runner GUI/API qualification. Rollback is removal of these additive read routes/UI changes and restoration of the previous verified artifact; no CloudStack schema migration is introduced. Production certification still requires real automatic cluster formation, storage survival, backup/restore and service-specific acceptance.

Source validation: 79 Kubernetes Python tests pass, including durable same-timestamp keyset pagination after SQLite reopen, foreign-project cursor denial, authorization-before-read, malformed/duplicate query fields, encoded native Kubernetes continuation, bounded pagination failure, namespace ownership, and node-pool recovery from native resources. Existing 74 tests remain covered. List limits bound one request to 100 operations or 1,000 native resources; no new state authority or schema migration. This is local Python 3.10 evidence, not Rocky live certification.

Image trust review: the existing resolver accepted any Ready KVM template, which does not establish the pinned universal RKE2 image required by sections 7–8 of the Kubernetes contract. Exact 4.22.1.1 `TemplateResponse.checksum` and `ChecksumValue.toString()` expose native checksums as `{algorithm}digest`. Add a server-owned, bounded map of qualified template IDs to SHA-256 checksums, deny missing/mismatched bindings before any CAPI write, and retain native Ready/KVM/project/Site discovery checks. An empty map is blocked; it must be populated only from independently qualified image evidence. This is a configuration gate, not evidence that a mapped image is actually qualified. Alternatives of trusting image names, UI flags, or arbitrary Ready templates fail this trust boundary. No template bytes, secrets or provider identity are copied into a second registry.

Browser transport integration: retained the existing restricted Gunicorn Unix socket and added a same-origin Nginx location without changing CloudStack routes/listeners. Official Nginx proxy URI/socket documentation was checked before implementation (https://nginx.org/en/docs/http/ngx_http_proxy_module.html#proxy_pass). A real local Nginx 1.18.0 process passed Unix-socket routing, query/cookie/origin/session-key/idempotency preservation, forged identity-header removal, no replay on a 503 mutation, JSON unknown-route rejection and unaffected native API location. Exact preliminary Ubuntu package SHA-256: `03e54de7c66335e5767cfd89c9a635389f9356900e3cf8279ee8dd6d78120a1e`; extracted without installation. Actual Rocky TLS/socket permissions/SELinux and served GUI remain NOT_TESTED for this route. Rollback removes only the added location include.

Combined UI source integration includes prior branch `codex/ui-completion` and GUI worker `b5fb730aaee8274013bff3f89b4415bbb1aea875`. All 22 UI suites / 318 tests and lint pass locally. The server-qualified image binding raises Python coverage to 81 passing tests; the separate real-Nginx test also passes. DBaaS/APaaS lifecycle remains unavailable, not certified by these tests.

The combined production UI build completed successfully and restored tracked public configuration to a clean worktree. A bounded hosted qualification lane now runs the same source against exact Rocky 9.8 amd64 OCI digest `sha256:ed654c694190a670bb5c33f68995b862214fc96ffbdca95e07a8935ba0a3717d`, resolved from the official Quay manifest on 2026-09-06. It records actual signed-RPM versions and runs tests unprivileged, including real QEMU/Nginx. This is a container compatibility check only; it cannot satisfy the required live Rocky VM/libvirt/GUI gates. No lab target or runtime credential is passed to this lane.

Native resource authorization review: browser discovery alone cannot authorize submitted IDs. The reconciler uses privileged provider credentials, so effective API grants plus project membership do not prove the caller can use a private service offering/template. Exact 4.22.1.1 ListServiceOfferingsCmd and ListTemplatesCmd derive from project/account-scoped list contracts. Before accepting browser create, validate every selected Zone/network/frontend/offering/template through the caller's native CloudStack session and project, with bounded IDs, an overall deadline and no credential persistence. Retain the reconciler's independent readiness/qualified-image checks. Deny when this verifier is absent, rather than allowing a default permissive adapter. This closes a concrete confused-deputy boundary without new RBAC authority or core edits.

Hosted source result: run `34047635554`, job `101525439557`, exact source `c43006aee53fa5b9b6c638bf48235dbde05e0d98` passed 82 Kubernetes + 27 DR tests without skips in Rocky 9.8. QEMU `10.1.0-17.el9_8.5`, Python `3.9.25-7.el9_8.3`, Nginx `1.20.1-28.el9_8.5.rocky.0.1`. Artifact `9993586910`, SHA-256 `5b9ed3043c127e9213de0a36dfdc29f383b4364a12459265a512024c59932398`.

CI failure diagnosis: existing generic UI workflow run `34047635516` selected Node16, which rejects the repository's OpenSSL legacy-provider option before compiling. Align this workflow with the already successful exact Node24.20.0 local/runner builds and committed npm lock; remove global moving CLI installs and npm-install lock mutation. No frontend dependencies are upgraded. Generic RAT run `34047635481` separately reports 182 unknown licenses across existing LayerSentry context, Single-OS, installer and candidate files; that release issue remains unresolved rather than being hidden by broad exclusions or an unapproved license change.
