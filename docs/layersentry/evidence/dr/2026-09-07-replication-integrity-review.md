# DR replication integrity review — 2026-09-07

Scope: isolated source/recovery/transport validation only. Integration base `b4a3ce53eaf885a48ee97375b25a3bc67558a4b1`, with the existing NAS, libvirt and interrupted-transfer source commits replayed as `45ee713616`, `3fc045947b`, `e30d499f7e`. No CloudStack core, Kubernetes source, runner or runtime changes are authorized by this record. Shared-session coordination is published on CloudStack PR #2.

## Receiver deadline defect and design

The receiver has a configured SIGALRM deadline, but its exception handler cancels that alarm before writing a buffered error frame. An authenticated peer that stops reading can fill stdout and block that error write indefinitely. Interpreter shutdown can also flush buffered bytes to the same blocked pipe. The initial targeted regression uses a real pre-filled OS pipe, a one-second configured timeout and an immutable synthetic recovery point; it checks bounded exit, released catalog lock and absence of publication. No remote endpoint is contacted.

Keep the existing fixed receiver protocol, authentication/scope contract, publication guards and read/copy deadline. Replace only receiver output with unbuffered, nonblocking descriptor writes bounded by the original monotonic deadline. Never extend an expired deadline merely to return diagnostic metadata. A peer that is not reading may receive no error frame; source-side disconnect remains an ambiguous transfer outcome resolved by replaying the same sealed epoch. Successful publication remains governed by the exact manifest receipt. This is preferable to adding another timeout, buffering more data, or exiting with `os._exit` that bypasses normal lock/context cleanup.

Relevant primary references: [Python signal semantics](https://docs.python.org/3/library/signal.html), [nonblocking descriptors](https://docs.python.org/3/library/os.html#os.set_blocking), [poll readiness](https://docs.python.org/3/library/select.html#polling-objects). This change adds no CloudStack API/provider behavior and retains the pinned 4.22.1.1 native adapter unchanged.

Threat/failure boundary: a paired authenticated sender controls pipe progress and request bytes but not receiver configuration. The asset is receiver availability and exclusive catalog access. Scope/manifest checks and private staging remain mandatory. An interrupted epoch is never automatically discarded or published. A later retry reuses verified sealed data. Kernel-blocked storage calls remain outside the claim of a user-space I/O deadline.

Baseline validation: all 27 existing DR tests passed locally with the reviewed native adapter and local QEMU tools configured, including full plus two incremental reconstructions, old/latest points, explicit zero extents and byte-for-byte recovered raw contents. Native API calls were offline fixtures; this is not live CloudStack recovery or Rocky certification. Targeted regression/final results follow when executed.

Receiver regression result: before the fix, the idle-sender and partial-disk tests passed, while a full unread stdout pipe exceeded the four-second outer test timeout despite the one-second configured receiver deadline. After the fix, all four receiver subprocess tests pass (idle sender, full output pipe, partial disk followed by successful retry, successful two-disk framed CLI transfer). The combined 31-test DR suite passes with zero skips, including real QEMU reconstruction. `git diff --check` passed. No hosted CI or Rocky test was executed by this root.

Local test inputs: Python `3.10.12`; QEMU `6.2.0` (Ubuntu `1:6.2+dfsg-2ubuntu6.31`); extracted `qemu-img` SHA-256 `52ab4a89383885ac4bf11812d69f5ecc77b238b8f9e708ad75f1cae821819bc0`; extracted `qemu-io` SHA-256 `e5d8628bff55ddc871ec6ba1b89190f782de9fa7e36f9bd443daca15dcda5f54`. Reviewed native fixture adapter comes from cozystack worktree `codex/dr-recovery-e2e` at `4ec0e59c4d84334ddb73fa3591ea2a710f6a3b7a`, file SHA-256 `085d179822fb94e6bc7e19a0623fb3d127d57bd813df0a6d6965e875657942c3`. Its API object is a local test stub; it made no CloudStack requests.

Command: `LAYERSENTRY_NATIVE_ACCEPTANCE_DIR=/home/opc/layersentry/dr-recovery-e2e/hack/layersentry LAYERSENTRY_TEST_QEMU_IMG=/tmp/layersentry-dr-qemu-tools/qemu-img LAYERSENTRY_TEST_QEMU_IO=/tmp/layersentry-dr-qemu-tools/qemu-io python3 -m unittest discover -s tools/layersentry/tests -p 'test_dr*.py' -v`.

Rollback: revert only the follow-up integrity-fix commit; no live rollback is required because this root has made no runtime mutations. Reverting the transport fix restores the known receiver hang, so it should not be deployed as a recovery strategy. Status remains PARTIAL until exact candidate CI and Rocky acceptance, native fixtures, failure/recovery and production gates pass.

## Exclusive lease defects and correction

Source review identified that lease timestamps were sampled before SQLite connection/lock waits. A transition could therefore accept an expired lease, and acquisition could return a lease whose TTL elapsed while waiting. Separately, `acquire_lease` returned an existing live token to another worker for the same operation ID, allowing duplicate executors to share and release one another's protection. A durable operation ID identifies work; it is not proof of current executor ownership.

Correction `d5cb78e38a` (reviewed from isolated agent commit `3164d9958108d2f154d33310f03b0fd10f8a2feb`) checks current time after the authoritative lease row/transaction lock is obtained and denies every acquisition of a live lease, including duplicate callers using the same operation ID. Explicit token-bearing renewal remains supported; sequential native coordinator invocations release and reacquire distinct tokens. No schema migration, CloudStack API change, native adapter edit, witness or fencing implementation is introduced.

Eight deterministic SQLite regression tests use real lock contention plus a controlled clock. Six failed against the previous implementation. All eight now pass, covering acquisition TTL, expired transition/renewal/release/read assertion, duplicate workers, sequential resume and stale-token rejection after replacement. The combined **39 DR tests pass with zero skips**, including the five native coordinator fixture cases and real QEMU old/latest reconstruction. `git diff --check` passes. These are local source checks, not `CI_VERIFIED`, `LIVE_VERIFIED` or `PRODUCTION_CERTIFIED`.

Receiver source fix: `fe03f92741`. Native adapter and fixture hashes match freshly fetched runner integration `c8ad45c4dca0755ba3091a4ee3f445e5ba9b8361`; fixture SHA-256 is `e14b828a4444ece4c8f2d8da14c99b2af21f0215499f63cf1f7840ccc9ff20dc`. No served UI or lab object was changed. Before live validation coordinate with the Kubernetes/runtime session and bind its acceptance run to this exact candidate; do not reuse earlier guest/capture results as evidence for these changes.

## Reproducible source gate

`tools/layersentry/tests/run_dr_integrity.py` requires the two native fixture hashes and real QEMU executables, rejects missing dependencies, skipped/expected-failure tests or fewer than 39 tests, and emits exact source/tool identities in a JSON result. Its local positive run passed all 39 cases; missing dependency configuration exits nonzero. The hosted-only `.github/workflows/layersentry-dr-integrity.yml` checks out exact PR source plus pinned public cozystack fixture source, installs distro QEMU on Ubuntu 22.04 and uploads the result. It has no self-hosted runner, lab credentials, deployment or package-publication step. Distro package resolution is recorded by actual binary version/hash; this source qualification is not a claim of a reproducible release artifact. The workflow is initially NOT_TESTED on GitHub until a real run completes.
